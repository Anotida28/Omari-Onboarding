# Frontend

This is a React + TypeScript frontend scaffold in a structure similar to the one you shared.

## Commands

- `npm install`
- `npm start`
- `npm run build`

## Environment

Create a `.env` file if you want to override the API URL:

```text
REACT_APP_API_BASE_URL=http://localhost:5000/api
```
